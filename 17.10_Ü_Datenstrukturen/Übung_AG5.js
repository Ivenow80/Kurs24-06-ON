// Erstellen Sie ein Set namens 'farbenSet' mit den Farben 'Gelb', 'Grün' und 'Blau'
let farbenSet = new Set(['Gelb', 'Grün', 'Blau']);

// Iterieren Sie mit einer for...of-Schleife und geben Sie jede Farbe im Format "Farbe: [Farbe]" aus
for (let farbe of farbenSet) {
    console.log(`Farbe: ${farbe}`);
}
