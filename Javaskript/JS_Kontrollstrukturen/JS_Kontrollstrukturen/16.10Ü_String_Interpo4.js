// Funktion, die einen Namen als Parameter nimmt und eine Begrüßung zurückgibt
function begruesse(name) {
    return `Willkommen, ${name}!`;
}

// Beispielaufruf der Funktion
let begruessung = begruesse("Ivonne");
console.log(begruessung);
