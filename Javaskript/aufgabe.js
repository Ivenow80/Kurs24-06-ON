<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Datentypen in JavaScript</title>
</head>
<body>

    <h1>JavaScript Datentypen</h1>

    <script>
        // Deklaration von Variablen mit verschiedenen Datentypen
        let age = 30; // Number
        let name = 'Max'; // String
        let isStudent = true; // Boolean
        let fruits = ['Apfel', 'Banane', 'Kirsche']; // Array
        let person = { // Object
            name: 'Anna',
            age: 25,
            isEmployed: false
        };

        // Ausgabe der Variablen in der Konsole
        console.log('Alter:', age); // 30 (Number)
        console.log('Name:', name); // Max (String)
        console.log('Ist Student:', isStudent); // true (Boolean)
        console.log('Früchte:', fruits); // ['Apfel', 'Banane', 'K
